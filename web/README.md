# Operator console

Next.js UI for the troubleshooting engine. From repo root:

```bash
python3 -m uvicorn app:app --host 0.0.0.0 --port 8765
cd web && npm install && npm run dev
```

Open http://127.0.0.1:43123
